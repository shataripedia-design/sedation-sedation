import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import PICUTool from "./PICUTool";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PICUTool />
  </React.StrictMode>
);
